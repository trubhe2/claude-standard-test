name: Sync Files
on:
  push:
    branches:
      - main
    paths:
      - README.md
      - .claude/**

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: BetaHuhn/repo-file-sync-action@v1
        with:
          GH_PAT: ${{ secrets.SYNC_PAT }}
